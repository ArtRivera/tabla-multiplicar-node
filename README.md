# Notas:

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -l, --list     Muestra la tabla de multiplicar      [boolean] [default: false]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -h, --hasta    Es el número al que llega la tabla       [number] [default: 10]```